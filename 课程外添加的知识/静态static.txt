static关键字
static意为静态，它可以用来修饰成员的变量、方法、代码块以及内部类
特点：
1.static所修饰的是随着类的加载而加载的
2.static是优先于对象所存在的
3.它所修饰的成员，是可以被所有对象所共享的(不属于对象，但是能被对象使用)
4.可以不创建对象，直接被类所调用
静态static最大的特点就是在内存中只有一份

在想调用创建好的static值时，也就可以不用创建对象，直接用类名去调用这个被static所修饰的值。
静态属性是类的所有对象所共享的，也就是说不管创建了多少个对象，静态属性在内部只有一份。